User-agent: *
Allow: /

# Search facet pages are not worth indexing — filter/sort permutations
# produce ~50K+ unique URLs that hit the DB on every request without caching.
# Detail pages are covered by the sitemap, so crawlers can still discover
# all canonical content.
Disallow: /search

# Explore-graph viewers pull a multi-MB unified graph JSON per request
# and accept arbitrary ?focus=<node-id> query params, giving them a huge
# crawl surface. The underlying entities are crawlable via their detail pages.
Disallow: /explore/unified
Disallow: /explore/neighborhoods

# Payload admin panel
Disallow: /admin/

# Allow the rate-limited API
Allow: /api/v1/

# AI crawlers (inherit the rules above; explicit groups for clarity)
User-agent: GPTBot
Allow: /
Disallow: /search
Disallow: /explore/unified
Disallow: /explore/neighborhoods
Disallow: /admin/

User-agent: ClaudeBot
Allow: /
Disallow: /search
Disallow: /explore/unified
Disallow: /explore/neighborhoods
Disallow: /admin/

User-agent: PerplexityBot
Allow: /
Disallow: /search
Disallow: /explore/unified
Disallow: /explore/neighborhoods
Disallow: /admin/

Sitemap: https://rmblknowledgecommons.org/sitemap.xml
